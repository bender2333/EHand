# Journal 2026-06-06 (architect / claude)

## 架构 v0.2.0 重写 + 契约 + 长循环协议

### 架构与契约
- 全新 `ARCHITECTURE.md`（v0.2.0）：模块化 + 26 张 Mermaid 图，取代旧 PRD 散文/辩论文档。
- 重心：Mock Engine 为差异化核心（§7，三层执行模型 + 声明式 MockModel + 脚本逃生舱 + 保真度 L1-L4）；
  SWD/analyzer/UART 为必做扎实的基础能力（§8）。
- 契约 0.1.0 → 0.2.0：Evidence Envelope 去冗余（身份只在 source/target_device）；
  新增 bottleneck + mock_fidelity；新建 mock_model.schema.json；版本全域同步 0.2.0。
- 新增 §15 AI-Native 服务分层（六层服务栈 + P9 能力面/推理面分离），§12 收敛为演化路线引用 §15。
- bug 修复：上一轮重写曾静默丢失 §8 整章（占位符冲突），已补回。

### 验证
- schema/TOML/fixture 全解析通过；fixture 过 evidence.schema.json v0.2.0（jsonschema）。
- compileall OK；pytest cli/tests 2 passed（含去冗余断言）。
- scenario run --replay --json = regression_pass，去冗余 envelope。
- 版本一致性 0.2.0：protocol.toml / schemas / ap_shared.h / package 全对齐。

### 长循环协议脚手架
- 角色厘清：CLAUDE.md = 架构师（我）；AGENTS.md = Codex worker。
- 模型：plan = goal = epic（D-1）；NORTH_STAR 瘦身为 Theme 不含 done（D-2，纠正硬伤）；
  硬件边界 hard-stop + 交接物（D-3）；自治连续推进（D-4）；work/v01a（D-5）；docs/plans/（D-6）。
- 新建 NORTH_STAR.md / LOOP.md / NEXT.md / docs/decision.md / docs/{plans,handoff}/ /
  docs/journal/{architect,codex}/。
- 清理 AGENTS.md children-story-site 残留 + CLAUDE.md journal 路径/空标题。

### 工程清理
- 删 _bmad/；归档 docx + 2 html 到 archive/；.gitignore 忽略 .codegraph/.serena/.cursor/.claude。

### 下一步
- 待出第一个 epic plan（建议 E1 = T-04/05/06）。本轮即将 commit 到 main（用户要求）。
