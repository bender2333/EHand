name: ci-protocol-sync

on:
  pull_request:
  push:

jobs:
  protocol-check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python cli/tools/generate_protocol.py --check
